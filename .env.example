# Server-side secret for the RAG backend (api/chat.ts). NEVER ships to the client bundle.
# Local dev:  copy to `.env.local` and fill in, then run `vercel dev`.
# Vercel:     set it in Project Settings -> Environment Variables (or `vercel env add`).
ANTHROPIC_API_KEY=
